bundle:
  name: volcano

workspace:
  root_path: /Shared/${bundle.name}
  file_path: /Shared/${bundle.name}/source
targets:
  dev: {}

variables:
  notebooks:
    description: Workspace notebook locations for the Volcano project.
    default:
      run_tests: /Repos/${workspace.current_user.userName}/volcano/notebooks/run_volcano_tests
